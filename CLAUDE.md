# CLAUDE.md

This project keeps a single source of truth for agent instructions in **[`AGENTS.md`](./AGENTS.md)**. Read it — it applies to Claude Code exactly as written.

Also read the nested `AGENTS.md` for the area you are touching:

- `packages/core/AGENTS.md`, `packages/providers/AGENTS.md`, `packages/quality/AGENTS.md`, `packages/cli/AGENTS.md`
- `apps/studio/AGENTS.md`, `apps/renderer/AGENTS.md`

Do not duplicate guidance here; update `AGENTS.md` instead so the two never drift.
